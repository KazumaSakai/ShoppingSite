package com.internousdev.ShoppingSite.util;

import java.util.Map;

import org.apache.struts2.interceptor.SessionAware;

public class CheckLogin implements SessionAware
{
	private static CheckLogin staticObj;
	private Map<String, Object>session;

	public static boolean IsLogin()
	{
		if(staticObj == null) staticObj = new CheckLogin();
		return staticObj._IsLogin();
	}

	public boolean _IsLogin()
	{
		Object isLogin_obj = session.get("isLogin");
		if(isLogin_obj == null)
		{
			return false;
		}
		return (boolean)isLogin_obj;
	}

	public Map<String, Object> getSession() {
		return session;
	}

	public void setSession(Map<String, Object> session) {
		this.session = session;
	}
}
